set(TARGET_NAME ${PROJECT_NAME})

file(GLOB CUDA_SRC_FILES
    ${CMAKE_CURRENT_SOURCE_DIR}/*.cu
    ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/*.c
)

cuda_add_library(${TARGET_NAME}_cuda STATIC ${CUDA_SRC_FILES})
